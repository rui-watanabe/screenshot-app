{
  "name": "common",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "del ./build",
    "build": "yarn run clean && tsc"
  },
  "license": "MIT",
  "dependencies": {
    "del-cli": "^3.0.1",
    "node-nats-streaming": "^0.3.2",
    "typescript": "^4.1.2"
  }
}
